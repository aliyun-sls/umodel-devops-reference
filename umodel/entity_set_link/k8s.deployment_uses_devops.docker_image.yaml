kind: entity_set_link
metadata:
  description:
    en_us: Deployment uses Docker Image.
    zh_cn: Deployment uses Docker Image。
  display_name:
    en_us: Deployment Uses Docker Image
    zh_cn: Deployment uses Docker Image
  domain: devops
  kind: entity_set_link
  name: k8s.deployment_uses_devops.docker_image
schema:
  url: umodel.aliyun.com
  version: v0.1.0
spec:
  dest:
    domain: devops
    kind: entity_set
    name: devops.docker_image
  entity_link_type: uses
  priority: 5
  src:
    domain: k8s
    kind: entity_set
    name: k8s.deployment
