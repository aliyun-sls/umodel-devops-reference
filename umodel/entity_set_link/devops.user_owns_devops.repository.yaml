kind: entity_set_link
metadata:
    description:
        en_us: User owns Repository.
        zh_cn: User owns Repository。
    display_name:
        en_us: User Owns Repository
        zh_cn: User owns Repository
    domain: devops
    kind: entity_set_link
    name: devops.user_owns_devops.repository
schema:
    url: umodel.aliyun.com
    version: v0.1.0
spec:
    dest:
        domain: devops
        kind: entity_set
        name: devops.repository
    entity_link_type: owns
    priority: 5
    src:
        domain: devops
        kind: entity_set
        name: devops.user
