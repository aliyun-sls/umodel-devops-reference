kind: entity_set_link
metadata:
    description:
        en_us: |
            Kubernetes DaemonSet uses container images, managing node-level service deployment relationships.
            Supports cluster-wide deployment tracking and version consistency management across nodes.
        zh_cn: |
            Kubernetes DaemonSet使用容器镜像，管理节点级服务部署关系。
            支持集群范围的部署追踪和跨节点版本一致性管理。
    display_name:
        en_us: Kubernetes DaemonSet Uses Container Images
        zh_cn: Kubernetes DaemonSet使用容器镜像
    domain: devops
    kind: entity_set_link
    name: k8s.daemonset_uses_devops.image
schema:
    url: umodel.aliyun.com
    version: v0.1.0
spec:
    dest:
        domain: devops
        kind: entity_set
        name: devops.image
    entity_link_type: uses
    priority: 5
    src:
        domain: k8s
        kind: entity_set
        name: k8s.daemonset
