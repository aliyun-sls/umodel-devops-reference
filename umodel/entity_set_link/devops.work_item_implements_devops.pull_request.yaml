kind: entity_set_link
metadata:
  description:
    en_us: Work Item implements Pull Request.
    zh_cn: Work Item implements Pull Request。
  display_name:
    en_us: Work Item Implements Pull Request
    zh_cn: Work Item implements Pull Request
  domain: devops
  kind: entity_set_link
  name: devops.work_item_implements_devops.pull_request
schema:
  url: umodel.aliyun.com
  version: v0.1.0
spec:
  dest:
    domain: devops
    kind: entity_set
    name: devops.pull_request
  entity_link_type: implements
  priority: 5
  src:
    domain: devops
    kind: entity_set
    name: devops.work_item
